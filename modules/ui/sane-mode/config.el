;;; sane-mode.el --- Description of the package -*- lexical-binding: t; -*-

;;; Commentary:
;; This file contains the 'sane-mode' package with functionalities:
;; - ...

;;; Code:

(use-package sane-mode
  :load-path "~/.repos/Sanemacs/lisp/sane-mode/"  ; Optional, since it's already in the load-path
  :config
  ;; Your custom configuration for sane-mode, if any
  ;; (toggle-sane-mode)
  )
